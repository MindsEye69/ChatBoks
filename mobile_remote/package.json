{
  "name": "chatboks-mobile-remote",
  "version": "0.1.0",
  "private": true,
  "description": "Android shell for the secure ChatBoks desktop remote bridge",
  "scripts": {
    "sync": "npx cap sync android",
    "copy": "npx cap copy android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
